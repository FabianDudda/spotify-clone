import { atom } from "recoil";

// export const playlistState = atom({
//   key: "playlistState",
//   default: null,
// });

export const episodeIdState = atom({
  key: "episodeIdState",
  default: "",
});
